/*
## INSTRUCTIONS

Complete the tasks below. 
Use node to run the tests in the test file found in this directory.

## TASKS

- Update `containsHello` with a function that takes a string and returns true if it contains the substring "hello", false otherwise. Make sure it's case-insensitive.
- Update `getInitials` with a function that takes a full name (e.g., "John Doe") and returns the initials (e.g., "JD"). Assume names are separated by a space.
- Update `getWordCount` with a function that takes a sentence (string) and returns the number of words in it. (Hint: split by space)
- Update `joinWordsIntoSentence` with a function that takes an array of words and joins them into a sentence with spaces in between. (Use .join(' '))
- Update `areAllNumbersPositive` with a function that takes an array of numbers and returns true if all numbers are positive, false otherwise.
- Update `isAnyNumberNegative` with a function that takes an array of numbers and returns true if any number is negative, false otherwise.
- Update `getNthFibonacciNumber` with a function that takes a number N and returns the Nth Fibonacci number (sequence starts 0, 1, 1, 2, 3...). (Can use a loop)
- Update `calculateFactorial` with a function that takes a number N and calculates its factorial (N!). (Use a loop)
- Update `calculatePowerByLoop` with a function that takes a base and an exponent and returns the result (alternative to **). (Use a loop)
- Update `isPalindrome` with a function that takes a string and returns true if it is a palindrome (reads the same forwards and backward, case-insensitive), false otherwise.
- Update `reverseArray` with a function that takes an array and returns a new array with the elements in reverse order. (Do not use the built-in .reverse() method - use a loop)
*/

// ---------------------------- //
// Fix the code below this line //
// ---------------------------- //

const containsHello = () => {};
const getInitials = () => {};
const getWordCount = () => {};
const joinWords = () => {};
const areAllNumbersPositive = () => {};
const isAnyNumberNegative = () => {};
const calculateFactorial = () => {};
const calculatePowerByLoop = () => {};
const isPalindrome = () => {};
const reverseArray = () => {};

// ------------------------------- //
// Don't edit code below this line //
// ------------------------------- //

let solution = {};

try {
	solution = {
		containsHello,
		getInitials,
		getWordCount,
		joinWords,
		areAllNumbersPositive,
		isAnyNumberNegative,
		calculateFactorial,
		calculatePowerByLoop,
		isPalindrome,
		reverseArray,
	};
} catch (error) {
	console.error(error);
}

export default solution;
