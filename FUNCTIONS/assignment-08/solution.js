/*
## INSTRUCTIONS

Complete the tasks below. 
Use node to run the tests in the test file found in this directory.

## TASKS

- Update `removeDuplicates` with a function that takes an array and returns a new array with duplicate elements removed.
- Update `mergeArrays` with a function that takes two arrays and returns a new array containing elements from both arrays.
- Update `getIntersection` with a function that takes two arrays and returns a new array containing only elements that appear in both arrays (intersection).
- Update `getKeys` with a function that takes an object and returns an array of its keys (['a', 'b']). (Use Object.keys())
- Update `getValues` with a function that takes an object and returns an array of its values ([1, 2]). (Use Object.values())
- Update `getByKey` with a function that takes an object and a key (string), and returns the value associated with that key.
- Update `getLength` with a function that takes an object and returns the number of key-value pairs it has.
*/

// ---------------------------- //
// Fix the code below this line //
// ---------------------------- //

const removeDuplicates = () => {};
const mergeArrays = () => {};
const getIntersection = () => {};
const getKeys = () => {};
const getValues = () => {};
const getByKey = () => {};
const getLength = () => {};

// ------------------------------- //
// Don't edit code below this line //
// ------------------------------- //

let solution = {};

try {
	solution = {
		removeDuplicates,
		mergeArrays,
		getIntersection,
		getKeys,
		getValues,
		getByKey,
		getLength,
	};
} catch (error) {
	console.error(error);
}

export default solution;
